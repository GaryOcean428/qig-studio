# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
*.so

# Envs
.venv/
venv/
.env

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Local checkpoints / artifacts (never committed)
checkpoints/
artifacts/
*.pt

# training run logs (reproducible, not source)
runs/

# derived everyday corpus — rebuilt from HuggingFace via scripts/build_everyday_corpus.py
# (reproducible, mixed upstream licenses; not source). None-safe: absent → academic-only.
data/everyday_corpus/

# derived rebuild corpus — regenerated by scripts/build_chat_corpus.py (mixed upstream licenses)
data/chat_corpus/

# downloaded parquet cache (full HF datasets, ~2.8GB) — reusable, not source
data/.parquet_cache/

# Local Claude Code agent worktree scaffolding (per-machine)
.claude/
